import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flipsoft.flipexpenses',
  appName: 'react-with-capacitor',
  webDir: 'build'
};

export default config;
